// Import jQuery
import * as $ from 'jquery';
(<any>window).jQuery = $;
(<any>window).$ = $;

// Countdown plugin
// import 'jquery-countdown';
// import 'jquery.maskedinput/src/jquery.maskedinput';

export default (<any>window).jQuery;
